import system.console

emptymap = []
map = [ one -> 1,
        pow -> n => exp => n^exp,
        top -> if true then 'kek' else 'lel' ]

console.trace(emptymap)
console.trace(map)
console.trace(map.pow(6))
